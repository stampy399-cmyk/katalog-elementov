# Отчёт: режим приёмки и секции «САНТЕХНИКА»

Дата: 2026-08-14

Репозиторий: `/Users/alphabravo/Downloads/katalog-elementov`

Страница: `https://stampy399-cmyk.github.io/katalog-elementov/#santehnika`

## Изменения

- Изменён `index.html`; `data.json` не менялся.
- Сверху вкладки добавлены крупная кнопка `ПРИЁМКА` и `ЭКСПОРТ РЕШЕНИЙ`.
- Полноэкранный режим показывает одну карточку: крупный кадр/клип, приём, машинное семейство, текущий русский раздел, tier, ручной чип, ролик и таймкод.
- Детерминированная очередь: сначала 63 карточки серой зоны, затем 693 карточки по жёсткому порядку русских разделов. Прогресс считается отдельно для этапов: `N из 63`, затем `N из 693`.
- Реализованы ответы `БАЗА`, `УНИК`, `МУСОР`, `РАЗДЕЛ НЕ ТОТ`, `ЭТО СКЛЕЙКА`, `ОК, ДАЛЬШЕ`.
- `РАЗДЕЛ НЕ ТОТ` содержит 6 простых разделов и спойлер со всеми 18 машинными семействами.
- `ЭТО СКЛЕЙКА` собирается кнопками-атомами; маленький ввод и Web Speech API оставлены только для необязательного нового атома.
- Решение хранится в существующей записи карточки в IndexedDB store `elements`, поле `acceptance`: `{id, verdict, family?, combo?, params?, ts}`. Новый store/вторая база не создавались.
- Сохранение выполняется существующим `saveElement(..., false)`/`commit`, поэтому работает индикатор `Сохранено`, общий экспорт JSON, автобэкап и merge «Обновить с сервера».
- `МУСОР` только ставит `verdict: "trash"`; карточка не удаляется.
- Отдельный экспорт скачивает `resheniya-priyomki.json` — массив только валидных решений.
- Повторный запуск продолжает с первой карточки без валидного решения.

## Секции

Порядок: `ПЕРЕХОДЫ` → `ЧАСТИЦЫ` → `ФУТАЖИ/КАРТИНКИ` → `ТИПОГРАФИКА` → `ГРАФИКА` → `ПРОЧЕЕ`.

Распределение при текущих ручных чипах:

- Переходы: 271; база 226; уник 25; серая 20.
- Частицы: 12; база 5; уник 7; серая 0.
- Футажи/картинки: 104; база 80; уник 15; серая 9.
- Типографика: 160; база 146; уник 3; серая 11.
- Графика: 171; база 130; уник 22; серая 19.
- Прочее: 38; база 25; уник 9; серая 4.
- Сумма: 756; база 612; уник 81; серая 63.

Ручной `category` определяет русский раздел раньше машинного семейства. Неизвестный ручной чип попадает в `ПРОЧЕЕ`. Липкое оглавление содержит 6 кнопок со счётчиками `всего/база/уник/серая`; клик прокручивает к разделу. Ленивая отрисовка карточек сохранена.

## QA

- `new Function(inlineScript)`: `1/1`, синтаксис без ошибок.
- `git diff --check`: ошибок нет.
- Google Chrome + Playwright CLI: `1 passed (3.3s)`.
- Консоль браузера: 0 ошибок; `pageerror`: 0.
- Встроенный self-test: failures `[]`.
- Очередь: 756 уникальных карточек; первые 63/63 имеют tier `gray`; после них серых нет.
- Секции: 6 заголовков в требуемом порядке; сумма `sectionSum = 756`.
- Fullscreen: одна `.acceptance-card`; первый счётчик `1 из 63`.
- IndexedDB: проверены `ok`, `family`, `combo`, `base`, `unique`, `trash`; 6/6 записей прочитаны обратно из store `elements`.
- Экспорт: скачан и распарсен JSON из 6 решений; у каждой записи валидны `id`, `verdict`, `ts`.
- Resume: после 6 решений повторный вход показал `7 из 63`.
- «Обновить с сервера»: после merge ранее записанное решение осталось в IndexedDB.
- Визуальный QA: `/tmp/katalog-priyomka-qa.png` (временный локальный скриншот, не коммитится).
- `data.json`: Git diff отсутствует; SHA-256 `ba83468ebdff50195af23749dc1d45eb4d62bb425abf4cc0a76af0e87ace97b0`.

## Git и GitHub Pages

- Функциональный коммит: `28e18cc` (`feat(santehnika): add acceptance mode`).
- Push: `origin/main`, успешно.
- Live URL: `https://stampy399-cmyk.github.io/katalog-elementov/index.html#santehnika`.
- Pages обновился на 6-й curl-проверке: HTTP `200`, 319202 байта, найдены обе сигнатуры `data-acceptance-open` и `SANTEHNIKA_SECTIONS`.
- Live-сайт обновлён: да.

## Неавтоматизированное

- Встроенный Browser был недоступен: 0 подключённых экземпляров; QA выполнен в локальном Google Chrome через Playwright CLI.
- Реальная запись с микрофона не автоматизировалась из-за системного разрешения; текстовый путь поля и код Web Speech API проверены. При недоступности API кнопка микрофона отключается, текстовый ввод остаётся рабочим.

## Доработка: YouTube-сегменты и «ЧТО КРУТИТСЯ»

### Реализация

- В `index.html` режим приёмки вместо статичного кадра показывает iframe `https://www.youtube.com/embed/<VIDEO_ID>` с параметрами `start`, `end`, `autoplay=1`, `mute=1`, `rel=0`.
- Границы сегмента вычисляются детерминированно: `start = floor(start - 1)`, `end = ceil(end + 1)`. Поддержаны диапазоны `M:SS(.s)–M:SS(.s)`, одиночные таймкоды и диапазоны в секундах.
- YouTube ID читается из `projects[].videoId/youtubeId/youtube_id`, затем из `projects[].url`. У всех 4 роликов ID найден в `url`, поэтому хардкод-маппинг не понадобился.
- Кнопка `ПОВТОРИТЬ ОТРЕЗОК` перезагружает тот же iframe через `about:blank` и следующий animation frame.
- Если ID отсутствует, остаётся существующий скриншот и выводится `нет видео`; если нет корректного таймкода — скриншот и `нет отрезка`.
- В обычной карточке добавлена кнопка `▶ МОМЕНТ НА YOUTUBE`; сегмент открывается во встроенном существующем лайтбоксе, где также доступен повтор.
- Под ответами добавлены независимые переключатели `ТЕКСТ`, `ЦИФРА`, `ЦВЕТ`, `РАЗМЕР`, `СКОРОСТЬ`, `ПОЗИЦИЯ`, `КАРТИНКА`.
- Выбранные значения сохраняются в том же решении как необязательный `params: [...]` в фиксированном порядке. Без выбранных чипов `ОК, ДАЛЬШЕ` работает и поле `params` не создаётся.
- `data.json` не изменён; SHA-256 прежний: `ba83468ebdff50195af23749dc1d45eb4d62bb425abf4cc0a76af0e87ace97b0`.

### Покрытие и QA

- Карточек сантехники с валидным YouTube ID и вычисленным сегментом: `756/756`; без ID: `0`; без сегмента: `0`.
- ID роликов: `nNgwfV-_ydM`, `QOUszAHEZAE`, `4QRQ90F0Iv8`, `zcHZn8y2f_s`; все 4 embed URL ответили HTTP `200`.
- Google Chrome + Playwright CLI: `1 passed (5.1s)`.
- Встроенный self-test: failures `[]`; синтаксис 5 inline-скриптов корректен; `git diff --check` без ошибок.
- Браузер подтвердил загрузку `#movie_player`, отсутствие `.ytp-error` и фактический `video.currentTime >= start`.
- Ошибки кода страницы в консоли: `0`; `pageerror`: `0`. Заблокированные тестовой сетью рекламные запросы YouTube/DoubleClick учитывались отдельно как внешние.
- Повтор iframe доказан наблюдением последовательности `src: исходный → about:blank → исходный` отдельно в приёмке и лайтбоксе.
- Чипы `ТЕКСТ` и `СКОРОСТЬ` записаны в существующий IndexedDB store `elements`; второе решение без чипов не получило `params`.
- Экспорт `resheniya-priyomki.json` распарсен: `params` присутствует только у решения с выбранными чипами.
- После `Обновить с сервера` сохранённый `params` остался в IndexedDB.
- Очередь/секции не регрессировали: `756` карточек, `63` серых первыми, сумма секций `756`.
- Визуальный QA: `/tmp/katalog-priyomka-video-qa.png` (временный файл, не коммитится).
- Встроенный Browser недоступен: список подключённых браузеров пуст; использован локальный Google Chrome.

### Git и GitHub Pages

- Функциональный коммит: `413133f` (`feat(santehnika): add segment playback`).
- Push `origin/main`: успешно.
- Live URL: `https://stampy399-cmyk.github.io/katalog-elementov/index.html#santehnika`.
- Pages обновился на 6-й curl-проверке: HTTP `200`, 337685 байт, найдены сигнатуры `data-youtube-segment-repeat`, `data-youtube-moment`, `ACCEPTANCE_PARAMS`.
- Живой сайт обновлён: да.

## Доработка: повтор ответа и атомы склейки

### Реализация

- Рядом с `ОК, ДАЛЬШЕ` добавлена крупная вторичная кнопка `ТАК ЖЕ, КАК ПРОШЛАЯ` с сигнатурой `data-repeat-last`.
- До первого успешного ответа текущей сессии кнопка disabled. После закрытия и нового входа сессионный прошлый ответ очищается, поэтому сохранённые ответы прошлой сессии случайно не повторяются.
- После успешного `saveElement(..., false)` в памяти сессии остаётся каноническое решение. Один клик на следующей карточке копирует `verdict`, `combo`, `family`, `params`, создаёт текущий `id`, новый монотонный `ts` и сохраняет через прежний store `elements`/поле `acceptance`.
- Подпись кнопки показывает действие, например `СКЛЕЙКА: тёплый фильтр · КРУТИТСЯ: ТЕКСТ`; для простых verdict выводятся `БАЗА`, `УНИК`, `МУСОР` или `ОК`.
- В `ЭТО СКЛЕЙКА` добавлен фиксированный набор из 9 атомов: `ТЁПЛЫЙ ФИЛЬТР`, `ГЛИТЧ`, `ЗАСВЕТКА`, `ЗУМ`, `ШУМ/ЗЕРНО`, `ПИКТОГРАММА`, `ТЕКСТ`, `ОВАЛ/ОБВОДКА`, `КРЕСТЫ`.
- Дополнительные атомы строятся из всех валидных сохранённых `acceptance.combo`: строки разбиваются по `+`, пробелы нормализуются, дубли удаляются без учёта регистра. Сначала всегда идут 9 стартовых атомов, затем сохранённые от новых к старым.
- Нажатие выбирает/снимает атом; массив сохраняет порядок нажатий. Живая строка и итоговый `combo` соединяют выбранные значения через ` + `.
- Поле `Своё слово (необязательно)` добавляет новый атом кнопкой или Enter; голосовой результат также сразу добавляется как выбранный атом.
- Схема IndexedDB, очередь, resume, экспорт, YouTube-сегменты, повтор отрезка и `params` не менялись.
- `data.json` не изменён; SHA-256: `ba83468ebdff50195af23749dc1d45eb4d62bb425abf4cc0a76af0e87ace97b0`.

### QA

- Google Chrome + Playwright CLI: `1 passed (10.9s)`.
- Встроенный self-test: failures `[]`; добавлены проверки стартового порядка атомов, дедупликации сохранённых атомов, порядка результата, полного набора repeat-полей без старых `id/ts` и текста подписи.
- Сценарий A: сохранено `{verdict: "ok", combo: "тёплый фильтр", params: ["ТЕКСТ"]}`.
- Сценарий B: один клик `ТАК ЖЕ, КАК ПРОШЛАЯ`; автопереход на C; IndexedDB подтвердил одинаковые `verdict/combo/params`, разные собственные `id` и `B.ts > A.ts`.
- История: атом `исторический атом` сохранён в C; после закрытия и нового входа он появился ровно одним невыбранным чипом, а repeat-кнопка снова disabled.
- Экспорт скачан и распарсен: решения A, B, C найдены, повторённые поля и пользовательский атом сохранены.
- Регрессии: очередь `756`, серых первыми `63`, сумма секций `756`, плееры `756/756`, без ID/сегмента `0/0`; `ПОВТОРИТЬ ОТРЕЗОК` сохранил последовательность `src → about:blank → src`.
- Консоль: `0` ошибок; `pageerror`: `0`; синтаксис 5 inline-скриптов корректен; `git diff --check` без ошибок.
- Визуальный QA: `/tmp/katalog-repeat-last-qa.png` (временный файл, не коммитится).
- Подключённый Chrome-канал Browser недоступен; по прямому ТЗ использован локальный Google Chrome через Playwright CLI.

### Git и GitHub Pages

- Функциональный коммит: `9d42fe3` (`feat(santehnika): repeat acceptance answers`).
- Push `origin/main`: успешно.
- Live URL: `https://stampy399-cmyk.github.io/katalog-elementov/index.html#santehnika`.
- Pages обновился на 6-й curl-проверке: HTTP `200`, 349938 байт, найдены `data-repeat-last`, `ACCEPTANCE_COMBO_ATOMS`, `ТАК ЖЕ, КАК ПРОШЛАЯ`.
- Живой сайт обновлён: да.
