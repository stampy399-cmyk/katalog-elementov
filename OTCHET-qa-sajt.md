# QA сайта

Дата: 2026-08-13

## Итог

- BUGS_FOUND=0
- FIXED=0
- Осталось: 0
- `index.html` и `sravnenie.html` не изменялись: дефекты не обнаружены.

## Проверено

- Встроенный JavaScript обеих страниц скомпилирован Node `vm.Script`: синтаксических ошибок 0.
- Полный Node mini-DOM прогон: 22 385 утверждений, ошибок и записей `console.error` 0.
- `data.json`: валидный JSON, 9 проектов, 1566 карточек, 18 семейств; дубликатов ID и битых ссылок `projectId` нет.
- У всех 1566 карточек валидны `family`, `freq_videos`, `freq_total`, `tier`; частоты и tier совпадают с пересчётом при пороге 50%; `freq_videos == 1` всегда `unique`.
- Фильтр: Все — 1566, База — 1483, Уникальные — 83; состояние кнопок и `aria-pressed` корректны.
- Панель статистики: KPI 1483/83/18/9 и все 18 семейных строк совпадают с `data.json`.
- `image=null` корректно нормализуется; все текущие 1566 путей изображений существуют.
- Пути с `?v=`: 26/26 сохраняют query и имеют рабочий fallback без `v`; оба варианта отданы локальным HTTP-сервером с кодом 200.
- Проект `4yavUTCeCp0`: 624/624 карточки рендерят `<img>`; файлы существуют, каждый больше 5 KiB; 624/624 отданы с HTTP 200.
- `sravnenie.html`: 18 строк и счётчик `18 семейств · 1566 карточки`; `our_media=null` даёт плашку; строковый и объектный `our_media.src`, видео и query-параметры рендерятся.
- Текущие `competitor_refs` и искусственно добавленные неизвестные вложенные поля проигнорированы без падения; `level=null` и `notes=null` безопасны.
- `index.html`, `sravnenie.html`, `data.json`, `sravnenie.json` отданы локальным HTTP-сервером с кодом 200.

Хедлесс-браузер в системе отсутствует, поэтому применён разрешённый условиями статический анализ и mini-DOM-тест на Node.

## QA-файл

- `scripts/qa-site-v1.js` — воспроизводимый прогон данных, встроенного JS и mini-DOM обеих страниц.
