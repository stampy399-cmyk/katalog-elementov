# SFX

Квалифицирующие группы отсутствуют: в `kirpichiki.json` нет ни одной группы с `family='Звук'` и `frequency>=2` в разных видео.

Запросы Epidemic, типы звуков и точки поиска в нише не добавлялись, поскольку исходных звуковых групп для анализа нет.
